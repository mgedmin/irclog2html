language: python
python:
    - 2.6
    - 2.7
    - 3.3
    - pypy
install:
    - pip install zope.testrunner --use-mirrors
    - pip install -e .
script:
    - zope-testrunner --test-path=src
notifications:
    email: false
